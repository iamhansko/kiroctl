---
inclusion: always
---

- Build output files must be located in `builds/`.
- Cross-platform builds use naming convention: `kiroctl`(Linux/macOS) or `kiroctl.exe`(Windows)

## Examples
- Windows : `builds/kiroctl.exe`
- Linux/macOS : `builds/kiroctl`